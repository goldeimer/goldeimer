/**
 * `false` type
 *
 * Used as a type parameter, this type alias is exactly equivalent to
 * `typeof false` at the callsite. Facilitates readability.
 */
export type False = false

/**
 * `true` type
 *
 * Used as a type parameter, this type alias is exactly equivalent to
 * `typeof true` at the callsite. Facilitates readability.
 */
export type True = true
