import * as walk from 'klaw'

/**
 * Walk down the subdirectory tree looking for the closest matching file.
 *
 * @remarks
 * Reexported from {@link klaw}.
 */
export { walk }

/**
 * Walk up the directory tree looking for the closest matching file.
 *
 * @remarks
 * Reexported from {@link walk-back}.
 */
export { default as walkBack } from 'walk-back'
