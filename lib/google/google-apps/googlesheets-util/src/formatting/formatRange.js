export const formatEmphasis = (
    range
) => range.setFontWeight('bold')

export const formatFootnote = (
    range
) => range.setFontSize(9)

export const formatTableName = (
    range
) => range.setFontSize(12)
