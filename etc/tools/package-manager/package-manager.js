import {
    compileTemplate,
    keyValuePairs,
    optionalKeyValuePair,
    renderTemplate,
    registerTemplate
} from '@goldeimer/template-engine'

const type = 'etc'
const subType = 'tools'
const pkgSlug = 'package-manger'

const DEFAULT_ARGS =

registerTemplate('')

const renderPackageJson = compileTemplate()
